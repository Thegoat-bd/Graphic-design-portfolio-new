<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Bernard Great Idiahi | Graphic Design Portfolio</title>
  <style>
    /* Reset & base */
    * {
      margin: 0; padding: 0; box-sizing: border-box;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      transition: background-color 0.3s, color 0.3s;
    }
    body {
      background: #f7f7f7;
      color: #222;
      line-height: 1.6;
      padding: 20px;
      scroll-behavior: smooth;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }
    a {
      color: #1e90ff;
      text-decoration: none;
    }
    a:hover {
      text-decoration: underline;
    }
    header, section {
      max-width: 900px;
      margin: 40px auto;
      background: white;
      padding: 30px;
      border-radius: 8px;
      box-shadow: 0 0 10px #ccc;
      opacity: 0;
      transform: translateY(30px);
      transition: opacity 0.6s ease-out, transform 0.6s ease-out, background-color 0.3s, color 0.3s;
    }
    header.visible, section.visible {
      opacity: 1;
      transform: translateY(0);
    }
    header {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 10px;
      position: relative;
    }
    header h1 {
      font-size: 2.8rem;
      margin-bottom: 5px;
    }
    header p {
      font-size: 1.2rem;
      color: #555;
      transition: color 0.3s;
    }
    h2 {
      margin-bottom: 20px;
      border-bottom: 2px solid #1e90ff;
      padding-bottom: 8px;
      font-weight: 600;
      transition: border-color 0.3s;
    }
    ul.skills-list {
      list-style: none;
      display: flex;
      flex-wrap: wrap;
      gap: 15px;
    }
    ul.skills-list li {
      background: #1e90ff;
      color: white;
      padding: 10px 15px;
      border-radius: 25px;
      font-weight: 600;
      font-size: 0.9rem;
      transition: background-color 0.3s ease;
      cursor: default;
    }
    ul.skills-list li:hover {
      background-color: #0f63c9;
    }
    .portfolio-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 25px;
    }
    .project {
      background: #fafafa;
      padding: 15px;
      border-radius: 8px;
      box-shadow: 0 0 5px #bbb;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      transition: box-shadow 0.3s ease, transform 0.3s ease, background-color 0.3s, color 0.3s;
      cursor: pointer;
    }
    .project:hover {
      box-shadow: 0 10px 20px rgba(30, 144, 255, 0.3);
      transform: translateY(-5px);
    }
    .project img {
      max-width: 100%;
      border-radius: 6px;
      margin-bottom: 12px;
      object-fit: cover;
      height: 180px;
    }
    .project h3 {
      font-size: 1.3rem;
      margin-bottom: 6px;
      color: #1e90ff;
      transition: color 0.3s;
    }
    .project p {
      font-size: 0.95rem;
      color: #333;
      flex-grow: 1;
      transition: color 0.3s;
    }
    .contact-info {
      font-size: 1rem;
      line-height: 1.4;
      margin-bottom: 15px;
      transition: color 0.3s;
    }
    .footer {
      text-align: center;
      margin: 40px 0;
      color: #777;
      font-size: 0.9rem;
      transition: color 0.3s;
    }
    /* Download resume button */
    #download-resume {
      position: absolute;
      right: 30px;
      top: 30px;
      background-color: #1e90ff;
      color: white;
      border: none;
      padding: 12px 20px;
      border-radius: 25px;
      font-weight: 600;
      cursor: pointer;
      transition: background-color 0.3s ease;
      font-size: 1rem;
      text-decoration: none;
    }
    #download-resume:hover {
      background-color: #0f63c9;
    }
    /* Dark mode toggle button */
    #dark-mode-toggle {
      position: absolute;
      left: 30px;
      top: 30px;
      background-color: #222;
      color: white;
      border: none;
      padding: 10px 16px;
      border-radius: 25px;
      font-weight: 600;
      cursor: pointer;
      transition: background-color 0.3s ease;
      font-size: 0.9rem;
    }
    #dark-mode-toggle:hover {
      background-color: #444;
    }
    /* Contact form styles */
    form {
      display: flex;
      flex-direction: column;
      gap: 15px;
    }
    form label {
      font-weight: 600;
      color: #222;
      transition: color 0.3s;
    }
    form input, form textarea {
      padding: 10px;
      border: 1.5px solid #ccc;
      border-radius: 6px;
      font-size: 1rem;
      transition: border-color 0.3s;
      resize: vertical;
    }
    form input:focus, form textarea:focus {
      outline: none;
      border-color: #1e90ff;
    }
    form button {
      width: 150px;
      background-color: #1e90ff;
      color: white;
      border: none;
      padding: 12px;
      border-radius: 25px;
      font-weight: 600;
      cursor: pointer;
      transition: background-color 0.3s ease;
      align-self: flex-start;
    }
    form button:hover {
      background-color: #0f63c9;
    }
    /* Responsive tweaks */
    @media (max-width: 600px) {
      header, section {
        padding: 20px;
        margin: 20px 10px;
      }
      #download-resume, #dark-mode-toggle {
        position: static;
        margin-top: 15px;
        width: 100%;
        text-align: center;
      }
      header {
        align-items: flex-start;
      }
      form button {
        width: 100%;
      }
    }

    /* Dark mode styles */
    body.dark {
      background: #121212;
      color: #eee;
    }
    body.dark header, 
    body.dark section {
      background: #1e1e1e;
      box-shadow: 0 0 15px #000;
      color: #eee;
    }
    body.dark header p {
      color: #ccc;
    }
    body.dark h2 {
      border-color: #5599ff;
    }
    body.dark ul.skills-list li {
      background: #5599ff;
    }
    body.dark ul.skills-list li:hover {
      background: #3366cc;
    }
    body.dark .project {
      background: #2a2a2a;
      box-shadow: 0 0 10px #000;
      color: #eee;
    }
    body.dark .project:hover {
      box-shadow: 0 10px 25px rgba(85, 153, 255, 0.6);
    }
    body.dark .project h3 {
      color: #5599ff;
    }
    body.dark .project p,
    body.dark .contact-info,
    body.dark form label {
      color: #ccc;
    }
    body.dark form input, body.dark form textarea {
      background: #333;
      border-color: #555;
      color: #eee;
    }
    body.dark form input:focus, body.dark form textarea:focus {
      border-color: #5599ff;
    }
    body.dark .footer {
      color: #999;
    }
    body.dark #download-resume {
      background-color: #5599ff;
      color: #121212;
    }
    body.dark #download-resume:hover {
      background-color: #3366cc;
      color: #fff;
    }
    body.dark #dark-mode-toggle {
      background-color: #444;
    }
    body.dark #dark-mode-toggle:hover {
      background-color: #666;
    }
  </style>
</head>

<body>
  <header class="visible">
    <h1>Bernard Great Idiahi</h1>
    <p>Graphic Design Portfolio</p>
    <button id="dark-mode-toggle">🌙 Dark Mode</button>
    <a href="files/resume.pdf" id="download-resume" download>Download Resume</a>
  </header>

  <section class="visible">
    <h2>Skills</h2>
    <ul class="skills-list">
      <li>Adobe Photoshop</li>
      <li>Adobe Illustrator</li>
      <li>UI/UX Design</li>
      <li>Branding</li>
      <li>Motion Graphics</li>
    </ul>
  </section>

  <section class="visible">
    <h2>Portfolio</h2>
    <div class="portfolio-grid">
      <!-- Example Project 1 -->
      <div class="project">
        <img src="https://via.placeholder.com/500x300" alt="Project Image">
        <h3>Logo Design for XYZ Company</h3>
        <p>A minimalist logo created for a start-up company focusing on eco-friendly products.</p>
      </div>
      <!-- Example Project 2 -->
      <div class="project">
        <img src="https://via.placeholder.com/500x300" alt="Project Image">
        <h3>Website Redesign for ABC Corp</h3>
        <p>Revamping an outdated website with modern aesthetics and improved user experience.</p>
      </div>
      <!-- Example Project 3 -->
      <div class="project">
        <img src="https://via.placeholder.com/500x300" alt="Project Image">
        <h3>Packaging Design for XYZ Product</h3>
        <p>Creative packaging design for a new beverage brand targeting millennials.</p>
      </div>
    </div>
  </section>

  <section class="visible">
    <h2>Contact</h2>
    <form>
      <label for="name">Name</label>
      <input type="text" id="name" placeholder="Your Name">
      
      <label for="email">Email</label>
      <input type="email" id="email" placeholder="Your Email">
      
      <label for="message">Message</label>
      <textarea id="message" rows="5" placeholder="Your Message"></textarea>
      
      <button type="submit">Send Message</button>
    </form>
  </section>

  <footer class="footer">
    <p>© 2025 Bernard Great Idiahi | All Rights Reserved</p>
  </footer>

  <script>
    // Dark Mode Toggle
    const darkModeButton = document.getElementById('dark-mode-toggle');
    darkModeButton.addEventListener('click', () => {
      document.body.classList.toggle('dark');
    });

    // Scroll Reveal Effect
    document.addEventListener('DOMContentLoaded', () => {
      const elements = document.querySelectorAll('header, section');
      setTimeout(() => {
        elements.forEach(el => el.classList.add('visible'));
      }, 100);
    });
  </script>
</body>
</html>
